"use client";import {useEffect,useState} from "react";import DoctorEngagementSignals from "@/doctor-dashboard/components/DoctorEngagementSignals";import type {EngagementSignal} from "@/types/engagementSignal";export default function DoctorSignalsPage(){const[signals,setSignals]=useState<EngagementSignal[]>([]);const[loading,setLoading]=useState(true);useEffect(()=>{fetch("/api/doctor/engagement-signals?patientId=ONK-DEMO-001").then(r=>r.json()).then(d=>setSignals(d.signals||[])).finally(()=>setLoading(false))},[]);return <main className="doctor-page"><div className="doctor-page-heading"><span className="doctor-eyebrow">ENGAGEMENT SIGNALS</span><h1>AI Engagement Review</h1><p className="doctor-muted-text">Review patient-coordination signals before any care-team action.</p></div>{loading?<p>Loading signals…</p>:<DoctorEngagementSignals signals={signals}/>}</main>}