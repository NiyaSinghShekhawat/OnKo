import {NextRequest,NextResponse} from "next/server";import {getDoctorEngagementSignals,reviewDoctorEngagementSignal} from "@/backend/api/engagementSignals";
export async function GET(req:NextRequest){try{const patientId=new URL(req.url).searchParams.get("patientId");if(!patientId)return NextResponse.json({error:"patientId is required."},{status:400});return NextResponse.json({signals:await getDoctorEngagementSignals(req,patientId)});}catch(e){return NextResponse.json({error:e instanceof Error?e.message:"Unable to load engagement signals."},{status:500})}}
export async function PATCH(req:NextRequest){try{const body=await req.json();if(!body.signalId||!["reviewed","dismissed"].includes(body.status))return NextResponse.json({error:"signalId and valid status are required."},{status:400});return NextResponse.json({signal:await reviewDoctorEngagementSignal(req,body.signalId,body.status,body.reviewNote)});}catch(e){return NextResponse.json({error:e instanceof Error?e.message:"Unable to review engagement signal."},{status:500})}}