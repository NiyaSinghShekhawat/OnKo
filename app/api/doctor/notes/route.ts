import {NextRequest,NextResponse} from "next/server";
import {requireDoctor} from "@/backend/api/auth";
import {createDoctorNote,listDoctorNotes,updateDoctorNote} from "@/backend/services/doctorNoteService";
import {getPatient} from "@/backend/services/patientService";
export async function GET(request:NextRequest){const a=await requireDoctor(request);if("error"in a)return a.error;const patientId=new URL(request.url).searchParams.get("patientId")||undefined;try{return NextResponse.json({data:await listDoctorNotes(a.doctorId,patientId)});}catch(e){return NextResponse.json({error:"Unable to load doctor notes."},{status:500});}}
export async function POST(request:NextRequest){const a=await requireDoctor(request);if("error"in a)return a.error;try{const b=await request.json();if(typeof b.patientId!=="string"||typeof b.title!=="string"||typeof b.content!=="string"||!b.title.trim()||!b.content.trim())return NextResponse.json({error:"Patient, title and content are required."},{status:400});const p=await getPatient(b.patientId);if(!p||p.doctorId!==a.doctorId)return NextResponse.json({error:"Patient not found."},{status:404});return NextResponse.json({data:await createDoctorNote({doctorId:a.doctorId,patientId:p.patientId,title:b.title,content:b.content})},{status:201});}catch(e){return NextResponse.json({error:e instanceof Error?e.message:"Unable to create note."},{status:400});}}
export async function PATCH(request:NextRequest){const a=await requireDoctor(request);if("error"in a)return a.error;try{const b=await request.json();if(typeof b.noteId!=="string")return NextResponse.json({error:"noteId is required."},{status:400});return NextResponse.json({data:await updateDoctorNote(a.doctorId,b.noteId,{title:typeof b.title==="string"?b.title:undefined,content:typeof b.content==="string"?b.content:undefined})});}catch(e){return NextResponse.json({error:e instanceof Error?e.message:"Unable to update note."},{status:400});}}
