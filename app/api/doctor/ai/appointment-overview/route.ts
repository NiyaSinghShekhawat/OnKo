import {NextRequest,NextResponse} from "next/server";import {getDoctorAppointmentOverview} from "@/backend/api/appointmentOverview";export async function GET(req:NextRequest){try{const url=new URL(req.url);const patientId=url.searchParams.get("patientId");const appointmentId=url.searchParams.get("appointmentId");if(!patientId||!appointmentId)return NextResponse.json({error:"patientId and appointmentId are required."},{status:400});return NextResponse.json(await getDoctorAppointmentOverview(req,patientId,appointmentId));}catch(e){return NextResponse.json({error:e instanceof Error?e.message:"Unable to create appointment overview."},{status:500})}}