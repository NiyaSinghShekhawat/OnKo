"use client";
import { PatientShell } from "@/patient-dashboard/components/PatientShell";
import { usePatientContext } from "@/app/patient/data-provider";

export default function TreatmentPage() {
  const { procedures, loading, error } = usePatientContext();
  return <PatientShell><main className="onko-page"><section className="patient-card"><span className="patient-eyebrow">TREATMENT DETAILS</span><h1 className="onko-page-title">Your treatment timeline</h1><p className="onko-page-subtitle">Procedures and treatment milestones recorded by your care team.</p></section><section className="patient-card"><div className="patient-card-heading"><div><span className="patient-eyebrow">PROCEDURES</span><h2 className="journey-section-title">Treatment history</h2></div><span className="patient-time">{procedures.length} records</span></div>{error ? <p className="patient-muted">{error}</p> : procedures.length ? <div className="clinical-list">{procedures.map((p)=><article className="clinical-list-item" key={p.procedureId}><div className="clinical-icon">✦</div><div className="clinical-main"><div className="clinical-title-row"><div><h3>{p.name}</h3><span>{p.date}</span></div><span className={`clinical-status clinical-status--${p.status === "completed" ? "done" : "scheduled"}`}>{p.status}</span></div><p>{p.details ?? "No additional details recorded."}</p>{p.purpose && <div className="clinical-purpose"><strong>Purpose</strong><span>{p.purpose}</span></div>}</div></article>)}</div> : <p className="patient-muted">{loading ? "Loading treatment records…" : "No treatment records have been added yet."}</p>}</section><section className="patient-safety-alert"><span className="patient-alert-icon">i</span><div><strong>Treatment information</strong><span>This page displays information entered by your care team. It does not independently interpret treatment or recommend changes.</span></div></section></main></PatientShell>;
}