"use client";
import { PatientShell } from "@/patient-dashboard/components/PatientShell";
import { usePatientContext } from "@/app/patient/data-provider";

export default function ReportsPage() {
  const { reports, loading, error } = usePatientContext();
  return <PatientShell><main className="onko-page"><section className="patient-card"><div className="patient-card-heading"><div><span className="patient-eyebrow">REPORTS & RECORDS</span><h1 className="onko-page-title">Your clinical records</h1><p className="onko-page-subtitle">Reports uploaded to your care record and their review status.</p></div></div></section><section className="patient-card"><div className="patient-card-heading"><div><span className="patient-eyebrow">DOCUMENT LIBRARY</span><h2 className="journey-section-title">Available records</h2></div><span className="patient-time">{reports.length} records</span></div>{error ? <p className="patient-muted">{error}</p> : reports.length ? <div className="report-list">{reports.map((r)=><article className="report-item" key={r.reportId}><div className="report-file-icon">PDF</div><div className="report-main"><div className="report-title-row"><div><h3>{r.title}</h3><span>{r.reportType ?? "Record"} · {r.uploadedAt}</span></div><span className={`clinical-status clinical-status--${r.status === "reviewed" ? "done" : "scheduled"}`}>{r.status === "reviewed" ? "Reviewed" : "Uploaded"}</span></div><p>{r.fileName}</p>{r.notes && <span className="report-note">{r.notes}</span>}</div><button type="button" className="report-view-button" disabled={!r.storagePath}>View</button></article>)}</div> : <p className="patient-muted">{loading ? "Loading records…" : "No reports have been uploaded yet."}</p>}</section><section className="patient-safety-alert"><span className="patient-alert-icon">i</span><div><strong>Report information</strong><span>Records are shown as uploaded or reviewed by your care team. OnKo does not independently interpret medical reports or provide a diagnosis.</span></div></section></main></PatientShell>;
}