"use client";import {useEffect,useState} from "react";
type Consent={status:"pending"|"opted-in"|"opted-out";consentedAt?:string;withdrawnAt?:string;version:string};
export default function WhatsAppConsent(){const[consent,setConsent]=useState<Consent|null>(null);const[loading,setLoading]=useState(true);const[busy,setBusy]=useState(false);const load=async()=>{try{setLoading(true);const r=await fetch("/api/patient/whatsapp-consent");const d=await r.json();setConsent(d.consent)}finally{setLoading(false)}};useEffect(()=>{load()},[]);const update=async(status:"opted-in"|"opted-out")=>{try{setBusy(true);const r=await fetch("/api/patient/whatsapp-consent",{method:"PATCH",headers:{"Content-Type":"application/json"},body:JSON.stringify({status})});const d=await r.json();setConsent(d.consent)}finally{setBusy(false)}};return <section className="patient-card"><span className="patient-eyebrow">WHATSAPP SYNC</span><h2>WhatsApp reminders</h2><p>Use WhatsApp for approved OnKo reminders and care-team updates. You can change this choice at any time.</p>{loading?<p>Loading preference…</p>:<><strong>Status: {consent?.status==="opted-in"?"Opted in":consent?.status==="opted-out"?"Opted out":"Not enabled"}</strong><div>{consent?.status==="opted-in"?<button disabled={busy} onClick={()=>update("opted-out")}>Turn off WhatsApp</button>:<button disabled={busy} onClick={()=>update("opted-in")}>Enable WhatsApp reminders</button>}</div></>}</section>}