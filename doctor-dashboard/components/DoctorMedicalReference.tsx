"use client";import {useState} from "react";import type {RAGResponse} from "@/types/rag";import {askDoctorMedicalReference} from "@/lib/api/doctorRag";export default function DoctorMedicalReference(){const[q,setQ]=useState("");const[r,setR]=useState<RAGResponse|null>(null);const[loading,setLoading]=useState(false);const[error,setError]=useState<string|null>(null);const ask=async()=>{if(!q.trim())return;try{setLoading(true);setError(null);setR(await askDoctorMedicalReference(q.trim()))}catch(e){setError(e instanceof Error?e.message:"Unable to retrieve medical reference.")}finally{setLoading(false)}};return <article className="doctor-card"><span className="doctor-eyebrow">AI MEDICAL REFERENCE</span><h2>Evidence-grounded reference assistant</h2><p className="doctor-muted-text">Searches public biomedical literature and grounds the response in retrieved sources. Doctor review required.</p><textarea value={q} onChange={e=>setQ(e.target.value)} placeholder="Ask a medical reference question…" rows={3}/><button onClick={ask} disabled={loading}>{loading?"Searching…":"Ask reference assistant"}</button>{error&&<p className="doctor-directory-error">{error}</p>}{r&&<div className="doctor-ai-insights"><div><strong>Reference answer</strong><p>{r.answer}</p></div><div><strong>Sources</strong><ol>{r.sources.map(s=><li key={s.sourceId}><a href={s.url} target="_blank" rel="noreferrer">{s.title}</a></li>)}</ol></div><small>{r.model} · {r.disclaimer}</small></div>}</article>}