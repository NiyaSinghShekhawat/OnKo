import type {RetrievalResult} from "@/types/rag";
export function buildMedicalRagPrompt(question:string,retrieval:RetrievalResult){const sources=retrieval.chunks.map((c,i)=>"[Source "+(i+1)+"]\n"+c.text).join("\n\n");return "You are a doctor-facing medical reference assistant. Answer only from the retrieved medical literature below. Do not diagnose a patient, prescribe treatment, recommend a patient-specific intervention, interpret a patient's medical report, calculate clinical risk, or modify a care plan. If the evidence is insufficient, say so. Distinguish evidence from uncertainty. Cite sources by their [Source N] labels. This is reference support for a qualified clinician, not a clinical decision.\n\nQUESTION:\n"+question+"\n\nRETRIEVED SOURCES:\n"+sources}