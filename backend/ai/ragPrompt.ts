import type { RetrievalResult } from "@/types/rag";
export function buildMedicalRagPrompt(question:string,retrieval:RetrievalResult){const sources=retrieval.chunks.map((c,i)=>"[Source "+(i+1)+"] "+(c.title||c.sourceId)+" | chunk "+c.chunkId+"\nSection: "+(c.section||"unknown")+"\nPublication date: "+(c.publicationDate||"unknown")+"\nEvidence:\n"+c.text).join("\n\n");return "You are a doctor-facing cancer-care medical reference assistant. Answer only from the retrieved biomedical literature below. Do not diagnose a patient, prescribe treatment, recommend a patient-specific intervention, interpret a patient's medical report, calculate clinical risk, or modify a care plan. If the evidence is insufficient or conflicting, say so. Prefer directly supported statements and distinguish evidence from uncertainty. Cite claims using the provided [Source N] labels. Do not invent citations or source numbers. If a claim cannot be supported by the retrieved evidence, omit it or state that the evidence is insufficient. This is reference support for a qualified clinician, not a clinical decision.\n\nQUESTION:\n"+question+"\n\nRETRIEVED CANCER-CARE LITERATURE:\n"+sources}