import type {Notification} from "@/types/notification";import {sendWhatsAppNotification} from "./whatsappAdapter";import {getWhatsAppConsent} from "@/backend/services/whatsappConsentService";import {updateNotificationDeliveryStatus} from "@/backend/services/notificationService";export async function deliverNotification(notification:Notification){if(notification.channel==="in-app"){await updateNotificationDeliveryStatus(notification.notificationId,"sent");return{status:"sent" as const};}const consent=await getWhatsAppConsent(notification.patientId);const result=await sendWhatsAppNotification(notification,consent);if(result.status==="sent")await updateNotificationDeliveryStatus(notification.notificationId,"sent");else if(result.status==="failed"||result.status==="not-consented")await updateNotificationDeliveryStatus(notification.notificationId,"failed");return result;}