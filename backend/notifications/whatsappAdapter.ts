import type {Notification} from "@/types/notification";import type {WhatsAppConsent} from "@/types/whatsappConsent";export interface WhatsAppDeliveryResult{status:"sent"|"not-configured"|"not-consented"|"failed";channel:"whatsapp";notificationId:string;reason?:string;}export async function sendWhatsAppNotification(notification:Notification,consent?:WhatsAppConsent|null):Promise<WhatsAppDeliveryResult>{if(!consent||consent.status!=="opted-in")return{status:"not-consented",channel:"whatsapp",notificationId:notification.notificationId,reason:"Patient has not opted in to WhatsApp notifications."};return{status:"not-configured",channel:"whatsapp",notificationId:notification.notificationId,reason:"WhatsApp transport is intentionally deferred for the current prototype."};}