import {retrieveMedicalLiterature} from "@/backend/rag";import {rankMedicalEvidence} from "@/backend/rag/evidenceRanker";import {answerMedicalReference} from "@/backend/ai/medicalReferenceProvider";
export async function askMedicalReference(question:string){const retrieval=await retrieveMedicalLiterature(question,5);const ranked=rankMedicalEvidence(question,retrieval.chunks,12);return answerMedicalReference(question,{...retrieval,chunks:ranked.map((c,i)=>({...c,rank:i+1}))})}