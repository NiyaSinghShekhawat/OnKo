import {getPatientCompanionContext} from "@/backend/ai/patientCompanionContext";import {generatePatientCompanion} from "@/backend/ai/patientCompanionModel";import {answerPatientMedicalQuestion} from "@/backend/ai/patientMedicalRag";import {createPatientAIConversation,listPatientAIConversations,persistPatientAIExchange} from "./patientAIConversationService";
const MEDICAL_PATTERN=/\b(what is|what are|explain|meaning|side effect|symptom|stage|cancer|chemotherapy|immunotherapy|radiotherapy|radiation|tumou?r|oncology|remission|metastatic|survivorship|palliative|screening|biopsy|pathology)\b/i;
export async function answerPatientCompanion(patientId:string,question:string,conversationId?:string){const context=await getPatientCompanionContext(patientId);const result=MEDICAL_PATTERN.test(question)?await answerPatientMedicalQuestion(question,context):await generatePatientCompanion(question,context);let conversation;if(conversationId){conversation=(await listPatientAIConversations(patientId)).find(x=>x.conversationId===conversationId);if(!conversation)throw new Error("Conversation not found.");}else conversation=await createPatientAIConversation(patientId,question.trim());await persistPatientAIExchange(patientId,conversation,question,result);return {...result,conversationId:conversation.conversationId};}
