import {getPatientCompanionContext} from "@/backend/ai/patientCompanionContext";import {generatePatientCompanion} from "@/backend/ai/patientCompanionModel";import {createPatientAIConversation,listPatientAIConversations,persistPatientAIExchange} from "./patientAIConversationService";
export async function answerPatientCompanion(patientId:string,question:string,conversationId?:string){const result=await generatePatientCompanion(question.trim(),await getPatientCompanionContext(patientId));let conversation;if(conversationId){conversation=(await listPatientAIConversations(patientId)).find(x=>x.conversationId===conversationId);if(!conversation)throw new Error("Conversation not found.");}else conversation=await createPatientAIConversation(patientId,question.trim());await persistPatientAIExchange(patientId,conversation,question,result);return {...result,conversationId:conversation.conversationId};}
