import {listDocuments} from "@/backend/firebase/firestore";import type {Patient,Appointment,Milestone} from "@/backend/types/firestore";import {generatePatientReminders} from "@/backend/services/reminderEngine";
export async function runReminderSweep(now=new Date()){const [patients,appointments,milestones]=await Promise.all([listDocuments<Patient>("patients"),listDocuments<Appointment>("appointments"),listDocuments<Milestone>("milestones")]);let created=0;for(const patient of patients){const a=appointments.filter(x=>x.patientId===patient.patientId);const m=milestones.filter(x=>x.patientId===patient.patientId);created+=(await generatePatientReminders(patient.patientId,a,m,now)).length}return {patients:patients.length,created}}