import {createDocument,listDocumentsByField} from "@/backend/firebase/firestore";import type {PatientAIConversation,PatientAIMessage} from "@/types/patientAIConversation";
export async function listPatientAIConversations(patientId:string){return(await listDocumentsByField<PatientAIConversation>("patientAIConversations","patientId",patientId)).sort((a,b)=>new Date(b.updatedAt).getTime()-new Date(a.updatedAt).getTime());}
export async function listPatientAIMessages(patientId:string,conversationId:string){const xs=await listDocumentsByField<PatientAIMessage>("patientAIMessages","patientId",patientId);return xs.filter(x=>x.conversationId===conversationId).sort((a,b)=>new Date(a.createdAt).getTime()-new Date(b.createdAt).getTime());}
export async function createPatientAIConversation(patientId:string,title:string){const now=new Date().toISOString();const data:PatientAIConversation={conversationId:crypto.randomUUID(),patientId,title:title.slice(0,120),createdAt:now,updatedAt:now};await createDocument("patientAIConversations",data);return data;}
export async function persistPatientAIExchange(patientId:string,conversation:PatientAIConversation,question:string,answer:{answer:string;evidence:string[];limitations:string[];model:string}){const now=new Date().toISOString();await createDocument("patientAIMessages",{messageId:crypto.randomUUID(),conversationId:conversation.conversationId,patientId,role:"patient",text:question,createdAt:now});await createDocument("patientAIMessages",{messageId:crypto.randomUUID(),conversationId:conversation.conversationId,patientId,role:"ai",text:answer.answer,evidence:answer.evidence,limitations:answer.limitations,model:answer.model,createdAt:new Date().toISOString()});return {...conversation,updatedAt:now};}
