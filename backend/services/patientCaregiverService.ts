import type {Caregiver,CaregiverAccessStatus} from "@/types/caregiver";import {getDocument,listDocumentsByField,setDocument} from "../firebase/firestore";
export function listCaregiversForPatient(patientId:string){return listDocumentsByField<Caregiver>("caregivers","patientId",patientId)}
export async function updateCaregiverForPatient(patientId:string,caregiverId:string,status:Extract<CaregiverAccessStatus,"consented"|"revoked">){const c=await getDocument<Caregiver>("caregivers",caregiverId);if(!c||c.patientId!==patientId)throw new Error("Caregiver not found.");if(c.accessStatus!=="invited"&&!(status==="revoked"&&c.accessStatus==="active"))throw new Error("Caregiver status cannot be changed from the current state.");const next={...c,accessStatus:status};await setDocument<Caregiver>("caregivers",caregiverId,next);return next}