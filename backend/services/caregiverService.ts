import type {Caregiver,CaregiverAccessStatus} from "@/types/caregiver";import {getDocument,listDocumentsByField,createDocument,setDocument} from "../firebase/firestore";
export function listCaregiversForPatient(patientId:string){return listDocumentsByField<Caregiver>("caregivers","patientId",patientId)}
export async function createCaregiverForDoctor(patientId:string,doctorId:string,input:Pick<Caregiver,"name"|"relationship"|"contact"|"permissions">){const p=await getDocument<{patientId:string;doctorId:string}>("patients",patientId);if(!p||p.doctorId!==doctorId)throw new Error("Patient not found.");const caregiver:Caregiver={caregiverId:crypto.randomUUID(),patientId,name:input.name,relationship:input.relationship,contact:input.contact,accessStatus:"invited",permissions:input.permissions};await createDocument<Caregiver>("caregivers",caregiver);return caregiver}
export async function updateCaregiverForDoctor(caregiverId:string,doctorId:string,status:CaregiverAccessStatus){const c=await getDocument<Caregiver>("caregivers",caregiverId);if(!c)throw new Error("Caregiver not found.");const p=await getDocument<{patientId:string;doctorId:string}>("patients",c.patientId);if(!p||p.doctorId!==doctorId)throw new Error("Caregiver not found.");const next={...c,accessStatus:status};await setDocument<Caregiver>("caregivers",caregiverId,next);return next}